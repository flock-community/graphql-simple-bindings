interface Account  {
  id: ID!
  lastLogin: DateTime!
}

type AccountPassword  implements Account{
  id: ID!
  lastLogin: DateTime!
}

type AccountKey implements Account {
  id: ID!
  lastLogin: DateTime!
  key: String!
}
